## Input
Our free-to-paid conversion is 3.1%. We think the paywall experience is the issue. Users hit the limit and the upgrade screen doesn't convert well. We're on a mobile app (iOS). Not sure of the exact drop-off breakdown.

## Expected output
Conversion flow map (draft — requires funnel breakdown):
Free usage → Usage limit hit → Upgrade screen shown → Payment conversion

The conversion flow isn't fully mappable without knowing: what percentage of free users hit the limit, and what percentage of those who see the upgrade screen actually convert.

3.1% of what? If this is 3.1% of all registered free users, the question is whether the issue is users not reaching the paywall or users reaching it but not converting. These require different fixes.

Friction points and drop-off analysis:
Cannot identify specific friction points without step-level data. Key questions:
- What percentage of free users hit the usage limit at least once? (If only 20% hit the limit, the paywall is barely exposed)
- Of users who see the upgrade screen, what percentage convert? (If it's 3.1% of views, that's a different diagnosis than 3.1% of registered users)
- Is the upgrade screen dismissible? Do users dismiss and never return?

Growth loop description:
Not provided. If there's a loop that paid conversion feeds (e.g., paid users invite teammates, or paid users generate content that attracts new users), optimizing conversion would strengthen it. Without knowing the growth model, optimization is isolated.

Optimization recommendations (preliminary, pending data):
1. Check paywall exposure rate before optimizing paywall copy — if most free users never see the upgrade screen, paywall conversion optimization won't move the top-level metric
2. Common iOS upgrade screen improvements: surface the specific value of paid (show exactly what becomes available), use a single CTA, reduce the number of pricing options shown at once
3. Consider a soft paywall (show a teaser of premium content) vs. hard paywall (block and force decision) — soft paywalls often convert better for B2C

Missing information needed to optimize effectively:
- Free user funnel breakdown: what % hit the limit? what % see the paywall? what % convert?
- What does the current upgrade screen look like? (copy, pricing display, CTA)
- Is there a trial option or is it direct pay?

Confidence level:
Context-informed
