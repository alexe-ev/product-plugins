---
description: Design a metric alerting system with thresholds, alert logic, and escalation paths so teams can detect performance issues before they become serious. Use this skill when a product needs proactive monitoring rather than reactive discovery.
---

Ask the user for: metrics to alert on, normal ranges or baselines for each metric. Optionally: historical metric variance data, SLA or reliability commitments, tooling available, on-call or response team structure.

Apply the skill at skills/design-metric-alert-system/SKILL.md.

If required inputs are missing, list them and stop. Output: alert inventory with thresholds, alert logic and frequency rules, escalation paths per severity level, response protocol per alert type, false positive mitigation approach, alert maintenance plan.
